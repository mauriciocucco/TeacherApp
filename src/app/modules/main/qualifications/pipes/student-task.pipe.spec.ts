import { StudentTaskPipe } from './student-task.pipe';

describe('StudentTaskPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentTaskPipe();
    expect(pipe).toBeTruthy();
  });
});
