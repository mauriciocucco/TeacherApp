import { StudentExamPipe } from './student-exam.pipe';

describe('StudentExamPipe', () => {
  it('create an instance', () => {
    const pipe = new StudentExamPipe();
    expect(pipe).toBeTruthy();
  });
});
